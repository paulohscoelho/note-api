# .env.example — copie para .env e preencha os valores reais
#
# Gere um JWT secret forte com:
#   openssl rand -base64 32

JWT_SECRET=
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=notesdb
DB_PORT=5432